# runtime e dados
# As duas primeiras são ancoradas na raiz de propósito: sem a barra inicial o
# padrão casa com qualquer pasta de qualquer nível, e `snapshots/` chegou a
# esconder do Git o pacote `application/use_cases/snapshots/`.
/data/
*.sqlite
*.sqlite-journal
.venv/
node_modules/
__pycache__/
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
.astro/

# local do agente
CLAUDE.local.md
.claude/settings.local.json

# pasta sincronizada (Drive) — saída, não fonte
/snapshots/
